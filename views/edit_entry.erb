<form method="get" action="/">
  <input type="submit" value="Home Screen"/>
</form>
<br>
<form method="post" action="/<%= @entry %>/edit">
  <table>
    <tr>
      <td>Date and Time</td>
      <td><input type="datetime-local" name="datetime" value="<%= parse_date_time_value(@entry) %>"></td>
    </tr>
    <tr>
      <td>Glucose</td>
      <td><input type="number" value="<%= parse_value(@entry, "glucose") %>" name="glucose" min="20" max="500"></td>
    </tr>
    <tr>
      <td>Carbs</td>
      <td><input type="number" value="<%= parse_value(@entry, "carbs") %>" name="carbs" min="0" max="100"></td>
    </tr>
    <tr>
      <td>Insulin</td>
      <td><input type="number" value="<%= parse_value(@entry, "insulin") %>" name="insulin" min="0" max="100"></td>
    </tr>
    <tr>
      <td></td>
      <td><input type="submit" value="Submit"></td>
    </tr>
  </table>
</form>
